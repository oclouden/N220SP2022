//Owen Louden, Intro Media Apps, 3/24/22

//so when greet function is enacted from the submit button,
function greet() {

    //take the information from this variable 'SelectedName'
    var name = (document.getElementById('selectedName'));

    //and display that information in the console
  console.log("Hello, " + name + "!");

  //now why is there an issue?
}
