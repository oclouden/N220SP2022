//finds the value in the input, also resets the value in the input box
var arry = document.getElementById(average).value = "";
window.onload = arry;


function calculateAverageOfArray(array) {
    var total = 0;
    var count = 0;

    //want to split the values from within the input
    
    //use a for loop to increase the count and total

    //math to do the average and sum



    return total / count;
}

console.log(calculateAverageOfArray(arry));
